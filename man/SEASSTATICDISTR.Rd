% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seas_staticdistr.R
\name{SEASSTATICDISTR}
\alias{SEASSTATICDISTR}
\title{Seasonal Static Parametric Distribution Mixture}
\usage{
SEASSTATICDISTR(
  formula,
  distr = c("pois", "nbinom"),
  hot_start = FALSE,
  w = NULL,
  ...
)
}
\arguments{
\item{formula}{Model specification.}

\item{distr}{Distribution family: \code{"pois"} (Poisson) or \code{"nbinom"}
(negative binomial).}

\item{hot_start}{Logical. If \code{TRUE}, leading zeros are removed from the
time series before fitting.}

\item{w}{Numeric in [0, 1] or \code{NULL} (default). The mixing weight for the
seasonal distribution. When \code{NULL}, \code{w} is selected automatically via
LOOCV.}

\item{...}{Not used.}
}
\value{
A model specification.
}
\description{
A mixture of two static parametric count distributions for intermittent
demand forecasting. When the time series has a seasonal period \code{m > 1}, the
predictive distribution at each horizon is a weighted combination of:
\itemize{
\item the \strong{full distribution}: fitted via method of moments on all past
observations;
\item the \strong{seasonal distribution}: fitted via method of moments on
observations at the same seasonal position only.
}
The mixing weight \code{w} in [0, 1] controls the contribution of the seasonal
component. If \code{w} is not supplied it is selected automatically via
leave-one-out cross-validation (LOOCV) on the log-score.
}
\details{
When the detected seasonal period is 1 (non-seasonal data), or when the
(trimmed) series length \code{n} is less than \code{2 * period}, the method falls back
to a single static distribution fitted on the full series (equivalent to
\code{\link[=STATICDISTR]{STATICDISTR()}}; a warning is issued). When \code{n >= 2 * period} but
\code{n < 3 * period}, LOOCV is skipped and \code{w} is set to \code{1 / period}.
}
\section{Method of moments}{

\strong{Poisson}: \eqn{\hat\lambda = \bar y}.
\strong{Negative binomial}: \eqn{\hat{p} = \bar y / s^2},
\eqn{\hat{r} = \bar y^2 / (s^2 - \bar y)}, where \eqn{s^2} is the sample
variance. If \eqn{s^2 \le \bar y} (underdispersed), falls back to Poisson.
}

\section{LOOCV details}{

LOOCV is only run when \code{n >= 3 * period}. Sufficient statistics
(\eqn{\sum y}, \eqn{\sum y^2}) are precomputed for
the full series and each seasonal sub-series. Each leave-one-out
distribution is then refitted in O(1) via closed-form MoM updates, making
each optimizer evaluation O(T). When a seasonal sub-series has only one
observation its leave-one-out counterpart is empty; the seasonal component
contributes zero probability for that observation, naturally penalising
\eqn{w > 0}.
}

