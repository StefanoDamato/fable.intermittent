% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twees_occ_damped_p0_fixed.R
\name{TWEES_OCC_DAMPED_p0_FIXED}
\alias{TWEES_OCC_DAMPED_p0_FIXED}
\alias{report.TWEES_OCC_DAMPED_p0_FIXED}
\title{Tweedie Exponential Smoothing with occurrence damping and fixed p0}
\usage{
TWEES_OCC_DAMPED_p0_FIXED(formula, damped = TRUE, scaling = TRUE, ...)

\method{report}{TWEES_OCC_DAMPED_p0_FIXED}(object, ...)
}
\arguments{
\item{formula}{Model specification.}

\item{damped}{Logical. If \code{TRUE} (default), the exponential smoothing
component uses a damping parameter.}

\item{scaling}{Logical. If \code{TRUE} (default), the time series is divided by
its maximum value before fitting and predictions are back-transformed.
This improves numerical stability.}

\item{...}{Not used.}

\item{object}{A fitted model object.}
}
\value{
A model specification.
}
\description{
As \code{TWEES_OCC_DAMPED}, except the occurrence process's initial state \code{p0}
is fixed at \code{clamp(mean(occurrence))} instead of being estimated by the
optimiser -- one fewer free parameter.
}
\examples{
ts <- tsibble::tsibble(
  time = as.Date("2026-01-01") + seq_len(40),
  value = rnbinom(40, size = 1, prob = 0.3),
  index = time
)

fc_ts <- ts |>
  model(TWEES_OCC_DAMPED_p0_FIXED(value)) |>
  forecast(h = "7 days")

fc_ts |> print()


if (requireNamespace("ggtime", quietly = TRUE)) {
  library(ggtime)
  fc_ts |> autoplot(ts)
}
}
\references{
Damato, S., Azzimonti, D., & Corani, G. (2025). Forecasting intermittent
time series with Gaussian Processes and Tweedie likelihood.
\emph{International Journal of Forecasting}  (in press).
\doi{10.1016/j.ijforecast.2025.10.001}.
}
