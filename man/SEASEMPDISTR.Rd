% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seas_empdistr.R
\name{SEASEMPDISTR}
\alias{SEASEMPDISTR}
\title{Seasonal Empirical Distribution Mixture}
\usage{
SEASEMPDISTR(formula, hot_start = FALSE, w = NULL, ...)
}
\arguments{
\item{formula}{Model specification.}

\item{hot_start}{Logical. If \code{TRUE}, leading zeros are removed from the
time series before fitting.}

\item{w}{Numeric in [0, 1] or \code{NULL} (default). The mixing weight for the
seasonal empirical distribution. When \code{NULL}, \code{w} is selected
automatically via LOOCV.}

\item{...}{Not used.}
}
\value{
A model specification.
}
\description{
A mixture of the full empirical distribution and a seasonal empirical
distribution for intermittent demand forecasting. When the time series has
a seasonal period \code{m > 1}, the predictive distribution at each horizon is a
weighted combination of:
\itemize{
\item the \strong{full empirical distribution}: built from all past observations
(equivalent to \code{\link[=EMPDISTR]{EMPDISTR()}});
\item the \strong{seasonal empirical distribution}: built only from observations
at the same seasonal position (e.g., same weekday for weekly data).
}
The mixing weight \code{w} in [0, 1] controls the contribution of the seasonal
component (\code{w = 0} → pure full empirical; \code{w = 1} → pure seasonal). If \code{w}
is not supplied it is selected automatically via leave-one-out
cross-validation (LOOCV) on the log-score.
}
\details{
When the detected seasonal period is 1 (non-seasonal data), the method
silently falls back to the plain empirical distribution.
}
\section{LOOCV details}{

For each observation \eqn{t}, the leave-one-out log-score is
\eqn{\log\bigl[(1-w)\,p^{-t}_{\text{full}}(y_t) + w\,p^{-t}_{\text{seas}}(y_t)\bigr]},
where \eqn{p^{-t}} denotes the empirical probability computed without
observation \eqn{t}. Observations for which both leave-one-out
probabilities are zero (i.e., \eqn{y_t} appears only once in the full
series and only once in its seasonal sub-series) are skipped. The total
log-score is maximised over \eqn{w \in [0,1]} using \code{\link[stats:optimize]{stats::optimize()}}.
}

